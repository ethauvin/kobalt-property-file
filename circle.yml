machine:
  java:
    version: oraclejdk8

dependencies:
  override:
     - chmod +x kobaltw

test:
  override:
    - ./kobaltw assemble test
  post:
    - cp -r kobaltBuild/test-output/* $CIRCLE_TEST_REPORTS/